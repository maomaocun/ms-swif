# ms-swift SFT 探索结论

日期：2026-05-24

## 一句话结论

当前最稳妥的路径是：9B 使用 Megatron TP=8/CP=1 已完成一次 262K 长上下文 SFT；27B Megatron TP=8/CP=1 可以启动并跑出 loss，但长上下文 step 很慢，适合后续长跑；27B 如果走 HF/Transformers + DeepSpeed 路径，应使用 `zero3_offload` 作为兜底，不再使用 `zero2_offload`。

## 训练目标与数据

- 数据集：`training/sft/ms-swift/data/paper2arm_qwen37_max_sft_reward_ge_0.6.jsonl`
- 当前数据统计：`size=48`，平均长度约 `70948.67` tokens，最短 `17723`，最长 `169996`。
- 长上下文设置：`max_length=262144`，`truncation_strategy=delete`。这个长度覆盖当前数据最大样本，目的就是避免截断轨迹。
- 学习率设置已统一：`lr=1e-5`，`min_lr=1e-6`，`warmup=0.05`。

## Loss 计算结论

训练使用 `--loss_scale default`。

当前格式下，核心行为是只让模型学习 assistant 侧要生成的内容，非模型生成的上下文被 mask 掉：

- system/user/tool_response 等上下文：labels 为 `-100`，不参与 loss。
- assistant 内容：labels 为 token id，参与 loss。
- 当前 ms-swift 模板会把 assistant 的思考、文本回复、bash/function call 结构串在 assistant 输出里，因此这些 assistant 输出会参与 loss。
- bash 命令返回结果属于环境反馈，即 tool_response/observation，不参与 loss。

日志中可以看到 label 预览类似：

```text
[INFO:swift] [LABELS] [-100 * 3366]<think>
```

这说明前面的上下文被 mask，进入 assistant 输出后开始计算 loss。

## 环境隔离结论

DeepSpeed/HF 和 Megatron 已拆成两个运行环境：

- HF/Transformers + DeepSpeed：使用 `training/sft/ms-swift/.venv`
- Megatron：使用 `training/sft/ms-swift/.venv-megatron`
- Megatron 启动前 source：`training/sft/ms-swift/megatron_env.sh`

`megatron_env.sh` 的作用是：

- 优先使用 `.venv-megatron/bin/python` 与 `.venv-megatron/bin/megatron`
- 复用 `.venv` 和全局环境里的 torch/deepspeed/transformers/CUDA wheel 依赖
- 设置 `PYTHONPATH`、`LD_LIBRARY_PATH`、`CUDNN_HOME`、`NVRTC_HOME`

所有新脚本、log、cache、outputs 都已统一放在：

```text
/mnt/cpfs/yangyicun/innovator-agent/training/sft/ms-swift
```

## Megatron 配置结论

Qwen3.5/Qwen3.6 当前在 Megatron Core 里使用 `gated_delta_net`/linear attention 变体。探索后结论：

- `context_parallel_size > 1` 当前不可用。
- 不能开 CP=8，也不能开 CP=4。
- 当前可用配置是 `TP=8, PP=1, CP=1, DP=1`，并打开 `sequence_parallel=true`。
- 显存优化使用：
  - `optimizer_cpu_offload=true`
  - `optimizer_offload_fraction=1`
  - `cross_entropy_loss_fusion=true`
  - 实际 fusion 实现为 `cross_entropy_fusion_impl='te'`

因此 Megatron 路径的有效长上下文配置是：

```text
tensor_model_parallel_size=8
pipeline_model_parallel_size=1
context_parallel_size=1
sequence_parallel=true
optimizer_cpu_offload=true
cross_entropy_loss_fusion=true
max_length=262144
```

## 9B Megatron 结果

脚本：

```text
training/sft/ms-swift/train_qwen35_9b_paper2arm_distill_megatron.sh
```

完整运行：

```text
qwen35-9b-paper2arm-distill-megatron-tp8-20260524-180145
```

结果：

- 已完成：`18/18`
- 最后一条 loss：`0.15988001`
- 记录峰值显存：`42.36 GiB`
- 最终速度记录：`111.867976 s/it`
- 已保存 checkpoint：

```text
training/sft/ms-swift/outputs/qwen35-9b-paper2arm-distill-megatron/qwen35-9b-paper2arm-distill-megatron-tp8-20260524-180145/checkpoint-18
```

指标文件：

```text
training/sft/ms-swift/outputs/qwen35-9b-paper2arm-distill-megatron/qwen35-9b-paper2arm-distill-megatron-tp8-20260524-180145/logging.jsonl
```

关键参数已确认：

```text
model=/mnt/cpfs/public_data/public_model/Qwen3.5/Qwen3.5-9B
max_length=262144
truncation_strategy=delete
loss_scale=default
tensor_model_parallel_size=8
context_parallel_size=1
sequence_parallel=True
optimizer_cpu_offload=True
optimizer_offload_fraction=1.0
cross_entropy_loss_fusion=True
cross_entropy_fusion_impl='te'
```

结论：9B Megatron 长上下文 SFT 已经实际跑通并产出 checkpoint。

## 27B Megatron 结果

脚本：

```text
training/sft/ms-swift/train_qwen36_27b_paper2arm_distill_megatron.sh
```

### Smoke

运行：

```text
qwen36-27b-paper2arm-distill-megatron-tp8-smoke-20260524-171947
```

结果：

- 已完成：`1/1`
- loss：`0.29181498`
- 显存记录：`19.67 GiB`
- 速度：`203.211875 s/it`
- 已保存 checkpoint：

```text
training/sft/ms-swift/outputs/qwen36-27b-paper2arm-distill-megatron/qwen36-27b-paper2arm-distill-megatron-tp8-smoke-20260524-171947/checkpoint-1
```

结论：27B Megatron TP=8/CP=1 基础链路可用。

### 262K 长上下文探索

运行：

```text
qwen36-27b-paper2arm-distill-megatron-tp8-20260524-173420
```

结果：

- 已跑出 loss，说明不是启动即失败。
- 已记录到 `4/18`：
  - step 1 loss：`0.20536338`
  - step 2 loss：`0.24138169`
  - step 3 loss：`0.26193354`
  - step 4 loss：`0.20705265`
- 显存记录：`53.84 GiB`
- 速度从 `509.881 s/it` 逐步到 `338.054361 s/it`
- 未保存 checkpoint，因为 `save_steps=50`，探索 run 在第 4 step 后停止。

结论：27B Megatron TP=8/CP=1 可以承载 262K 长上下文并跑出 loss，显存不是当前主要问题；主要问题是单 step 时间长。如果要继续 27B 长训，这条路径是可继续的，但需要接受较长 wall time。

## HF/DeepSpeed 27B 结论

脚本：

```text
training/sft/ms-swift/train_qwen36_27b_paper2arm_distill_full_sp4.sh
```

当前脚本默认：

```text
DEEPSPEED=zero3_offload
SEQUENCE_PARALLEL_SIZE=4
CELOSS_PARALLEL_SIZE=2048
padding_free=true
use_logits_to_keep=false
max_length=262144
```

探索结论：

- `SEQUENCE_PARALLEL_SIZE=8` 不可用。Qwen3.5/Qwen3.6 linear attention 在 HF 路径下不支持 derived ring attention，日志建议降到 SP=4。
- `SEQUENCE_PARALLEL_SIZE=4 + zero2_offload` 在 27B 262K loss 计算处 OOM。日志显示单卡接近 79GiB 后仍尝试分配 500MiB。
- 因此 HF/DeepSpeed 路径应直接使用 `zero3_offload`。

结论：27B 如果不用 Megatron，而是继续走 HF/Transformers + DeepSpeed，推荐路径是 SP=4 + `zero3_offload`，不要再试 SP=8 或 zero2_offload。

## 当前推荐用法

9B 长上下文训练：

```bash
bash training/sft/ms-swift/train_qwen35_9b_paper2arm_distill_megatron.sh
```

27B Megatron 长上下文训练：

```bash
bash training/sft/ms-swift/train_qwen36_27b_paper2arm_distill_megatron.sh
```

27B HF/DeepSpeed 兜底：

```bash
DEEPSPEED=zero3_offload bash training/sft/ms-swift/train_qwen36_27b_paper2arm_distill_full_sp4.sh
```

不建议继续尝试：

- Megatron `CP=4/8`
- HF/DeepSpeed `SEQUENCE_PARALLEL_SIZE=8`
- 27B HF/DeepSpeed `zero2_offload`

## 当前机器状态

最后检查时：

- 无 screen 训练会话。
- 无残留 SFT/Megatron 训练进程。
- 8 张 GPU 显存占用为 0，训练任务已退出。

## 最终判断

目标“尝试使用 Megatron 跑起来目前的两个，并隔离 DeepSpeed 与 Megatron 环境”已经完成：

- 9B：Megatron 262K 全量跑完并保存 checkpoint。
- 27B：Megatron smoke 跑通；262K 长上下文也已跑出多步 loss，证明链路可用。
- 环境：HF/DeepSpeed 与 Megatron 已分离；相关脚本、log、output、cache 已统一在 `training/sft/ms-swift` 下。
- 后续 27B 正式长训建议优先 Megatron TP=8/CP=1；若回 HF/DeepSpeed，直接 zero3_offload。
